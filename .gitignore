node_modules
dist
out
build
!packages/app/build
release
*.log
*.tsbuildinfo
.DS_Store
Thumbs.db
.env
.env.local
packages/app/src/renderer/dist
# OpenPLC Runtime source — downloaded by the CI workflow before Docker build;
# never committed (large C/C++ build tree). To build locally, run:
#   mkdir -p containers/openplc/openplc-src
#   curl -fsSL https://github.com/thiagoralves/OpenPLC_v3/archive/refs/heads/master.tar.gz \
#     | tar -xz --strip-components=1 -C containers/openplc/openplc-src
containers/openplc/openplc-src/
# ICS Lab scenarios are distributed via Canvas — keep off the public repo.
scenarios/ICS_Lab_01.otflab
scenarios/ICS_Lab_02.otflab
# Old tutorial filenames from before the rename — no longer tracked.
scenarios/tutorial-01-modbus-coil-write.otflab
scenarios/tutorial-02-scenario-builder.otflab
