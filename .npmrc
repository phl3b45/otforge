engine-strict=false
legacy-peer-deps=true
# Do not write or update package-lock.json. The lockfile is untracked (see
# .gitignore) to avoid cross-platform `git pull` conflicts; this makes every
# npm install skip it, not just the predev hook.
package-lock=false
